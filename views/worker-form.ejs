<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .header h1 { font-size: 28px; margin-bottom: 5px; }
        .header p { font-size: 14px; opacity: 0.9; }
        .form-container { padding: 40px 30px; }
        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }
        .form-group { margin-bottom: 20px; }
        .form-group.full-width { grid-column: 1 / -1; }
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #333;
            font-weight: 600;
            font-size: 14px;
        }
        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 15px;
            transition: all 0.3s ease;
            font-family: inherit;
        }
        .form-group textarea {
            resize: vertical;
            min-height: 100px;
        }
        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }
        .form-group small {
            display: block;
            margin-top: 5px;
            color: #666;
            font-size: 12px;
        }
        .btn-submit {
            width: 100%;
            padding: 14px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
        }
        .error-message {
            background: #fee;
            color: #c33;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 14px;
            border-left: 4px solid #c33;
        }
        .links {
            text-align: center;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #e0e0e0;
        }
        .links a {
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
            margin: 0 10px;
        }
        .links a:hover { color: #764ba2; }
        .checkbox-group {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .checkbox-group input[type="checkbox"] {
            width: auto;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>💼 İş Arayan Platformu</h1>
            <p><%= pageTitle %></p>
        </div>
        
        <div class="form-container">
            <% if (error) { %>
                <div class="error-message"><%= error %></div>
            <% } %>

            <form action="<%= actionUrl %>" method="POST">
                <% if (!worker) { %>
                <!-- GİRİŞ BİLGİLERİ (Sadece yeni kayıtta) -->
                <div class="form-group full-width" style="background: #f0f9ff; padding: 20px; border-radius: 10px; margin-bottom: 30px;">
                    <h3 style="color: #0369a1; margin-bottom: 15px; font-size: 18px;">🔐 Giriş Bilgileri</h3>
                    
                    <div class="form-group" style="margin-bottom: 15px;">
                        <label for="email">E-posta Adresi *</label>
                        <input type="email" id="email" name="email" 
                               placeholder="ornek@email.com"
                               required>
                        <small>Bu e-posta adresiyle sisteme giriş yapacaksınız</small>
                    </div>
                    
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                        <div class="form-group" style="margin-bottom: 0;">
                            <label for="password">Şifre *</label>
                            <input type="password" id="password" name="password" 
                                   placeholder="••••••••"
                                   minlength="6"
                                   required>
                            <small>En az 6 karakter</small>
                        </div>
                        
                        <div class="form-group" style="margin-bottom: 0;">
                            <label for="confirmPassword">Şifre Tekrar *</label>
                            <input type="password" id="confirmPassword" name="confirmPassword" 
                                   placeholder="••••••••"
                                   minlength="6"
                                   required>
                        </div>
                    </div>
                </div>
                
                <h3 style="color: #667eea; margin-bottom: 20px; font-size: 18px;">👤 Kişisel Bilgiler</h3>
                <% } %>
                
                <div class="form-row">
                    <div class="form-group">
                        <label for="firstName">Ad *</label>
                        <input type="text" id="firstName" name="firstName" 
                               value="<%= worker ? worker.firstName : '' %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="lastName">Soyad *</label>
                        <input type="text" id="lastName" name="lastName" 
                               value="<%= worker ? worker.lastName : '' %>" required>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="birthDate">Doğum Tarihi *</label>
                        <input type="date" id="birthDate" name="birthDate" 
                               value="<%= worker && worker.birthDate ? new Date(worker.birthDate).toISOString().split('T')[0] : '' %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="phone">Telefon *</label>
                        <input type="tel" id="phone" name="phone" 
                               placeholder="0555 123 4567"
                               value="<%= worker ? worker.phone : '' %>" required>
                    </div>
                </div>

                <div class="form-group full-width">
                    <label for="address">Adres *</label>
                    <input type="text" id="address" name="address" 
                           placeholder="Şehir, İlçe"
                           value="<%= worker ? worker.address : '' %>" required>
                </div>

                <h3 style="color: #667eea; margin: 30px 0 20px 0; font-size: 18px;">💼 Kariyer Bilgileri</h3>

                <div class="form-row">
                    <div class="form-group">
                        <label for="profession">Meslek/Uzmanlık *</label>
                        <input type="text" id="profession" name="profession" 
                               placeholder="Örn: Elektrikçi, Aşçı, Yazılımcı"
                               value="<%= worker ? worker.profession : '' %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="jobType">İş Türü *</label>
                        <select id="jobType" name="jobType" required>
                            <option value="Tam Zamanlı" <%= worker && worker.jobType === 'Tam Zamanlı' ? 'selected' : '' %>>Tam Zamanlı</option>
                            <option value="Yarı Zamanlı" <%= worker && worker.jobType === 'Yarı Zamanlı' ? 'selected' : '' %>>Yarı Zamanlı</option>
                            <option value="Serbest" <%= worker && worker.jobType === 'Serbest' ? 'selected' : '' %>>Serbest</option>
                            <option value="Staj" <%= worker && worker.jobType === 'Staj' ? 'selected' : '' %>>Staj</option>
                            <option value="Geçici" <%= worker && worker.jobType === 'Geçici' ? 'selected' : '' %>>Geçici</option>
                        </select>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group">
                        <label for="experienceYears">Deneyim (Yıl) *</label>
                        <input type="number" id="experienceYears" name="experienceYears" 
                               min="0" max="50"
                               value="<%= worker ? worker.experienceYears : 0 %>" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="education">Eğitim Durumu *</label>
                        <select id="education" name="education" required>
                            <option value="İlkokul" <%= worker && worker.education === 'İlkokul' ? 'selected' : '' %>>İlkokul</option>
                            <option value="Ortaokul" <%= worker && worker.education === 'Ortaokul' ? 'selected' : '' %>>Ortaokul</option>
                            <option value="Lise" <%= worker && worker.education === 'Lise' ? 'selected' : '' %>>Lise</option>
                            <option value="Üniversite" <%= worker && worker.education === 'Üniversite' ? 'selected' : '' %>>Üniversite</option>
                            <option value="Yüksek Lisans" <%= worker && worker.education === 'Yüksek Lisans' ? 'selected' : '' %>>Yüksek Lisans</option>
                            <option value="Doktora" <%= worker && worker.education === 'Doktora' ? 'selected' : '' %>>Doktora</option>
                        </select>
                    </div>
                </div>

                <div class="form-group full-width">
                    <label for="skills">Yetenekler/Beceriler</label>
                    <input type="text" id="skills" name="skills" 
                           placeholder="Virgülle ayırın: Excel, İngilizce, Sürücü Belgesi"
                           value="<%= worker && worker.skills ? worker.skills.join(', ') : '' %>">
                    <small>Virgülle ayırarak birden fazla yetenek ekleyebilirsiniz</small>
                </div>

                <div class="form-group full-width">
                    <label for="expectedSalary">Beklenen Maaş (TL)</label>
                    <input type="number" id="expectedSalary" name="expectedSalary" 
                           min="0" step="100"
                           placeholder="Örn: 17500"
                           value="<%= worker ? worker.expectedSalary : '' %>">
                </div>

                <div class="form-group full-width">
                    <label for="about">Hakkımda / Özgeçmiş Özeti</label>
                    <textarea id="about" name="about" 
                              placeholder="Kendiniz hakkında kısa bir bilgi yazın..."
                              maxlength="1000"><%= worker ? worker.about : '' %></textarea>
                    <small>Maksimum 1000 karakter</small>
                </div>

                <% if (worker) { %>
                <div class="form-group full-width checkbox-group">
                    <input type="checkbox" id="isActive" name="isActive" 
                           <%= worker && worker.isActive ? 'checked' : '' %>>
                    <label for="isActive" style="margin: 0;">İş aramaya devam ediyorum</label>
                </div>
                <% } %>

                <button type="submit" class="btn-submit">
                    <%= worker ? 'Güncelle' : 'Kayıt Ol' %>
                </button>
            </form>
            
            <div class="links">
                <% if (worker) { %>
                    <a href="/workers/profile">Profilime Dön</a>
                <% } else { %>
                    <a href="/auth/login">Zaten hesabım var, giriş yap</a>
                <% } %>
            </div>
        </div>
    </div>

    <% if (!worker) { %>
    <script>
        // Şifre eşleşme kontrolü
        document.querySelector('form').addEventListener('submit', function(e) {
            const password = document.getElementById('password').value;
            const confirmPassword = document.getElementById('confirmPassword').value;
            
            if (password !== confirmPassword) {
                e.preventDefault();
                alert('Şifreler eşleşmiyor!');
                document.getElementById('confirmPassword').focus();
            }
        });
    </script>
    <% } %>
</body>
</html>
